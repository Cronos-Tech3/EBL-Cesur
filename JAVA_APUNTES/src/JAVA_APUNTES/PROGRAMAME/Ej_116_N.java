package JAVA_APUNTES.PROGRAMAME;

import java.util.Scanner;

public class Ej_116_N {
    public static void main(String[] args) {

        /*  Wrong answer
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        if (n < 0 || n > 5) {
            System.out.println("El número debe estar entre 0 y 5.");
        } else {
            for (int i = 0; i < n; i++) {
                System.out.println("Hola Mundo.");
            }
        }
        scanner.close()
         */
      Scanner sc = new Scanner(System.in);
      //La entrada consta de una única línea que contiene un número n, 0 ≤ n ≤ 5, que indica cuántos mensajes hay que emitir.
      //La Salida Cada mensaje a escribir aparecerá en una única línea y será la cadena "Hola mundo.".

    }
}
