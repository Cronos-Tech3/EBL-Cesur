package JAVA_APUNTES.A_Javadoc.soluciones_Paloma.Simulacro_Pruebas_RA4_6.Prueba5_Metodos;

/*
public class dienteConColecciones {




         HashSet<Diente> dientes=new HashSet<>();

        // Método para agregar un diente
        public boolean agregarDiente(String nombre, int numero) {
            Diente diente = new Diente(nombre, numero);
            return dientes.add(diente); // Retorna true si se agregó, false si ya existe
        }
        // Método para obtener un diente
        public Diente obtenerDiente(String nombre) {
            for (Diente diente : dientes) {
                if (diente.getNombre().equals(nombre)) {
                    return diente; // Devuelve el diente si lo encuentra
                }
            }
            return null; // Devuelve null si no existe
        }



         ArrayList<Diente> dientes1=new ArrayList<>();

        // Método para agregar un diente
        public boolean agregarDiente1(String nombre, int numero) {
            for (Diente diente : dientes1) {
                if (diente.getNombre().equals(nombre)) {
                    return false; // No agrega si ya existe un diente con ese nombre
                }
            }
            dientes1.add(new Diente(nombre, numero));
            return true;
        }
        // Método para obtener un diente
        public Diente obtenerDiente1(String nombre) {
            for (Diente diente : dientes1) {
                if (diente.getNombre().equals(nombre)) {
                    return diente; // Devuelve el diente si lo encuentra
                }
            }
            return null; // Devuelve null si no existe
        }


    private Stack<Diente> dientes2=new Stack<>();

    // Método para agregar un diente
    public boolean agregarDiente2(String nombre, int numero) {
        for (Diente diente : dientes2) {
            if (diente.getNombre().equals(nombre)) {
                return false; // No agrega si ya existe
            }
        }
        dientes2.push(new Diente(nombre, numero)); // Agrega al stack
        return true;
    }
    // Método para obtener un diente
    public Diente obtenerDiente2(String nombre) {
        for (Diente diente : dientes2) {
            if (diente.getNombre().equals(nombre)) {
                return diente; // Devuelve el diente si lo encuentra
            }
        }
        return null; // Devuelve null si no existe
    }

    private LinkedList<Diente> dientes3=new LinkedList<>();

    // Método para agregar un diente
    public boolean agregarDiente4(String nombre, int numero) {
        for (Diente diente : dientes) {
            if (diente.getNombre().equals(nombre)) {
                return false; // No agrega si ya existe
            }
        }
        dientes.add(new Diente(nombre, numero));
        return true;
    }
    // Método para obtener un diente
    public Diente obtenerDiente4(String nombre) {
        for (Diente diente : dientes) {
            if (diente.getNombre().equals(nombre)) {
                return diente; // Devuelve el diente si lo encuentra
            }
        }
        return null; // Devuelve null si no existe
    }


}

 */













